name: build
description: build
inputs:
  build-type:
    description: 'type of build'
    required: true
    default: 'dev'
runs:
  using: "composite"
  steps:
    - run: yarn build:${{ inputs.build-type }}
      shell: bash
